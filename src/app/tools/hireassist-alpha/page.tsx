import HireAssistAlpha from "./HireAssistAlpha";

export const metadata = {
  title: "HireAssist NL (Alpha) — CubeA",
  description: "Netherlands tech job aggregator — alpha preview.",
};

export default function Page() {
  return <HireAssistAlpha />;
}
